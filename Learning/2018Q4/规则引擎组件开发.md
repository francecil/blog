```
let str = `复制小幺鸡上内容`
obj = {}
str.split('\n').forEach(v=>{
	let item = v.split(':')[0]
	let list = item.split('.')
	if(!obj[list[1]]){
		obj[list[1]]={}
	} 
	obj[list[1]][list[2].trim()]=item.trim()
})
fe json 工具 JSON.stringify(obj)
vscode types.constant 中替换并格式化
正则替换：'(.*)':  =》  $1:
```

国际化文件中处理：

```js
obj2 = {}
str.split('\n').forEach(v=>{
	let item = v.split(':')[0]
	let list = item.split('.')
	if(!obj2[list[1]]){
		obj2[list[1]]={}
	} 
	obj2[list[1]][list[2].trim()]=v.split(':')[1].trim()
})
```